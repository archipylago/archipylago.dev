{% if sponsors.length > 0 %}
<section>
  <h2>We are working with</h2>
  <div id="sponsors">
    {% for sponsor in sponsors | sort(false, true, 'name') %} {{ sponsor |
    formatSponsorLogo | safe }} {% endfor %}
  </div>
</section>
<hr />
{% endif %}
